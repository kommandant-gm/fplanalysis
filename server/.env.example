PORT=5000
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=fpl_analysis

# Security
ADMIN_USERNAME=admin
ADMIN_PASSWORD=change-this-password
ADMIN_SECRET=change-this-to-a-long-random-signing-secret
ADMIN_TOKEN_TTL_SECONDS=43200

# Comma-separated list of allowed browser origins for API access.
# For local Vite dev, defaults are already allowed if this is empty.
# Example production:
# CORS_ORIGINS=https://your-client.up.railway.app,https://yourdomain.com
CORS_ORIGINS=
